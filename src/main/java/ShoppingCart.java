public class ShoppingCart {
    // uses eager initialization since even when empty, most sites lets you view an empty cart
    // cart has items added to it, removed from it, keeps list of items visible with prices, and has running total
    //
}
